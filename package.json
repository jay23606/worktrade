{
  "name": "worktrade",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python -m http.server 8080",
    "test": "node --check app.js && node --check modules/store.js && node --check modules/agreements.js && node --check modules/backend.js && node --test tests/*.test.mjs"
  }
}
